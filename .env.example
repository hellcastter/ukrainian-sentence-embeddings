# Trainer loads .env with python-dotenv. Do not commit credentials.
# Only needed when log_to_wandb=True in the training INI.
WANDB_API_KEY=
# Set wandb_entity and wandb_project_name in your INI to an account you can use.
# Generation currently uses BASE_URL/API_KEY/MODEL_NAME constants in its script;
# it does not read this file. No paid API account is required for its local server.
